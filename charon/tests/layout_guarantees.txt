test_crate::SimpleStruct
original Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<u32>, offset(1) + SizeOf<u32>, offset(2) + SizeOf<u32>), ALIGN),
    ALIGN >= max(AlignOf<u32>, AlignOf<u32>, AlignOf<u32>),
    offset of 
        0.0 is aligned to AlignOf<u32>,
        0.1 is aligned to AlignOf<u32>,
        0.2 is aligned to AlignOf<u32>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<u32>, offset(1) + SizeOf<u32>, offset(2) + SizeOf<u32>), ALIGN),
    ALIGN >= max(AlignOf<u32>, AlignOf<u32>, AlignOf<u32>),
    offset of 
        0 is aligned to AlignOf<u32>,
        1 is aligned to AlignOf<u32>,
        2 is aligned to AlignOf<u32>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0) + 4usize, offset(1) + 4usize, offset(2) + 4usize), 4usize),
    ALIGN >= 4usize,
    offset of 
        0.0 is aligned to 4usize,
        0.1 is aligned to 4usize,
        0.2 is aligned to 4usize,
}

test_crate::GenericStruct
original Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<usize>, offset(1) + SizeOf<T>), ALIGN),
    ALIGN >= max(AlignOf<usize>, AlignOf<T>),
    offset of 
        0.0 is aligned to AlignOf<usize>,
        0.1 is aligned to AlignOf<T>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<usize>, offset(1) + SizeOf<T>), ALIGN),
    ALIGN >= max(AlignOf<usize>, AlignOf<T>),
    offset of 
        0 is aligned to AlignOf<usize>,
        1 is aligned to AlignOf<T>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0) + 8usize, offset(1) + SizeOf<T>), max(8usize, AlignOf<T>)),
    ALIGN >= max(8usize, AlignOf<T>),
    offset of 
        0.0 is aligned to 8usize,
        0.1 is aligned to AlignOf<T>,
}

test_crate::UnsizedStruct
original Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<usize>, offset(1) + SliceLength), ALIGN),
    ALIGN >= max(AlignOf<usize>, AlignOf<usize>),
    offset of 
        0.0 is aligned to AlignOf<usize>,
        0.1 is aligned to AlignOf<usize>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<usize>, offset(1) + SliceLength), ALIGN),
    ALIGN >= max(AlignOf<usize>, AlignOf<usize>),
    offset of 
        0 is aligned to AlignOf<usize>,
        1 is aligned to AlignOf<usize>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0) + 8usize, offset(1) + SliceLength), 8usize),
    ALIGN >= 8usize,
    offset of 
        0.0 is aligned to 8usize,
        0.1 is aligned to 8usize,
}

test_crate::SimpleEnum
original Guarantees: {
    SIZE >= aligned_to(max(0usize, 0usize), ALIGN),
    ALIGN >= max(1usize, 1usize),
}
direct Guarantees: {
    SIZE >= aligned_to(max(0usize, 0usize), ALIGN),
    ALIGN >= max(1usize, 1usize),
}
normalized Guarantees: {
    SIZE >= 0usize,
    ALIGN >= 1usize,
}

test_crate::SimpleAdt
original Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<usize>, offset(1.1) + SizeOf<usize>, offset(2.0) + SizeOf<u32>, offset(2.1) + SizeOf<u32>), ALIGN),
    ALIGN >= max(1usize, AlignOf<usize>, AlignOf<usize>, AlignOf<u32>, AlignOf<u32>),
    offset of 
        1.0 is aligned to AlignOf<usize>,
        1.1 is aligned to AlignOf<usize>,
        2.0 is aligned to AlignOf<u32>,
        2.1 is aligned to AlignOf<u32>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<usize>, offset(1.1) + SizeOf<usize>, offset(2.0) + SizeOf<u32>, offset(2.1) + SizeOf<u32>), ALIGN),
    ALIGN >= max(1usize, AlignOf<usize>, AlignOf<usize>, AlignOf<u32>, AlignOf<u32>),
    offset of 
        1.0 is aligned to AlignOf<usize>,
        1.1 is aligned to AlignOf<usize>,
        2.0 is aligned to AlignOf<u32>,
        2.1 is aligned to AlignOf<u32>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + 8usize, offset(1.1) + 8usize, offset(2.0) + 4usize, offset(2.1) + 4usize), 8usize),
    ALIGN >= 8usize,
    offset of 
        1.0 is aligned to 8usize,
        1.1 is aligned to 8usize,
        2.0 is aligned to 4usize,
        2.1 is aligned to 4usize,
}

test_crate::NicheAdt
original Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>), ALIGN),
    ALIGN >= max(1usize, AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>),
    offset of 
        1.0 is aligned to AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>), ALIGN),
    ALIGN >= max(1usize, AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>),
    offset of 
        1.0 is aligned to AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>), max(1usize, AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>)),
    ALIGN >= max(1usize, AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>),
    offset of 
        1.0 is aligned to AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>,
}

test_crate::IsAZST
original Guarantees: {
    SIZE = 0usize,
    ALIGN = 1usize,
}
direct Guarantees: {
    SIZE = 0usize,
    ALIGN = 1usize,
}
normalized Guarantees: {
    SIZE = 0usize,
    ALIGN = 1usize,
}

test_crate::GenericWithKnownLayout
original Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<usize>, offset(1) + SizeOf<Box<T, Global>[TraitClause0::ImpliedClause0, {built_in impl Sized for Global}, impl_Allocator_for_Global]>), ALIGN),
    ALIGN >= max(AlignOf<usize>, AlignOf<Box<T, Global>[TraitClause0::ImpliedClause0, {built_in impl Sized for Global}, impl_Allocator_for_Global]>),
    offset of 
        0.0 is aligned to AlignOf<usize>,
        0.1 is aligned to AlignOf<Box<T, Global>[TraitClause0::ImpliedClause0, {built_in impl Sized for Global}, impl_Allocator_for_Global]>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<usize>, offset(1) + SizeOf<Box<T, Global>[TraitClause0::ImpliedClause0, {built_in impl Sized for Global}, impl_Allocator_for_Global]>), ALIGN),
    ALIGN >= max(AlignOf<usize>, AlignOf<Box<T, Global>[TraitClause0::ImpliedClause0, {built_in impl Sized for Global}, impl_Allocator_for_Global]>),
    offset of 
        0 is aligned to AlignOf<usize>,
        1 is aligned to AlignOf<Box<T, Global>[TraitClause0::ImpliedClause0, {built_in impl Sized for Global}, impl_Allocator_for_Global]>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0) + 8usize, offset(1) + SizeOf<Box<T, Global>[TraitClause0::ImpliedClause0, {built_in impl Sized for Global}, impl_Allocator_for_Global]>), max(8usize, AlignOf<Box<T, Global>[TraitClause0::ImpliedClause0, {built_in impl Sized for Global}, impl_Allocator_for_Global]>)),
    ALIGN >= max(8usize, AlignOf<Box<T, Global>[TraitClause0::ImpliedClause0, {built_in impl Sized for Global}, impl_Allocator_for_Global]>),
    offset of 
        0.0 is aligned to 8usize,
        0.1 is aligned to AlignOf<Box<T, Global>[TraitClause0::ImpliedClause0, {built_in impl Sized for Global}, impl_Allocator_for_Global]>,
}

test_crate::Reordered
original Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<u8>, offset(1) + SizeOf<u32>, offset(2) + SizeOf<u8>), ALIGN),
    ALIGN >= max(AlignOf<u8>, AlignOf<u32>, AlignOf<u8>),
    offset of 
        0.0 is aligned to AlignOf<u8>,
        0.1 is aligned to AlignOf<u32>,
        0.2 is aligned to AlignOf<u8>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<u8>, offset(1) + SizeOf<u32>, offset(2) + SizeOf<u8>), ALIGN),
    ALIGN >= max(AlignOf<u8>, AlignOf<u32>, AlignOf<u8>),
    offset of 
        0 is aligned to AlignOf<u8>,
        1 is aligned to AlignOf<u32>,
        2 is aligned to AlignOf<u8>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0) + 1usize, offset(1) + 4usize, offset(2) + 1usize), 4usize),
    ALIGN >= 4usize,
    offset of 
        0.0 is aligned to 1usize,
        0.1 is aligned to 4usize,
        0.2 is aligned to 1usize,
}

test_crate::NotReordered
original Guarantees: {
    SIZE = aligned_to(offset(2) + SizeOf<u8>, ALIGN),
    ALIGN = max(AlignOf<u8>, AlignOf<u32>, AlignOf<u8>),
    offset of 
        0.0 is 0,
        0.1 is computed by repr(C) algorithm,
        0.2 is computed by repr(C) algorithm,
}
direct Guarantees: {
    SIZE = aligned_to(offset(2) + SizeOf<u8>, ALIGN),
    ALIGN = max(AlignOf<u8>, AlignOf<u32>, AlignOf<u8>),
    offset of 
        0 is 0,
        1 is computed by repr(C) algorithm,
        2 is computed by repr(C) algorithm,
}
normalized Guarantees: {
    SIZE = 12usize,
    ALIGN = 4usize,
    offset of 
        0.0 is 0,
        0.1 is computed by repr(C) algorithm,
        0.2 is computed by repr(C) algorithm,
}

test_crate::Packed
original Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<u8>, offset(1) + SizeOf<u32>, offset(2) + SizeOf<u8>), ALIGN),
    ALIGN = min(1usize, max(AlignOf<u8>, AlignOf<u32>, AlignOf<u8>)),
    offset of 
        0.0 is aligned to min(1usize, AlignOf<u8>),
        0.1 is aligned to min(1usize, AlignOf<u32>),
        0.2 is aligned to min(1usize, AlignOf<u8>),
}
direct Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<u8>, offset(1) + SizeOf<u32>, offset(2) + SizeOf<u8>), ALIGN),
    ALIGN = min(1usize, max(AlignOf<u8>, AlignOf<u32>, AlignOf<u8>)),
    offset of 
        0 is aligned to min(1usize, AlignOf<u8>),
        1 is aligned to min(1usize, AlignOf<u32>),
        2 is aligned to min(1usize, AlignOf<u8>),
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0) + 1usize, offset(1) + 4usize, offset(2) + 1usize), 1usize),
    ALIGN = 1usize,
    offset of 
        0.0 is aligned to 1usize,
        0.1 is aligned to 1usize,
        0.2 is aligned to 1usize,
}

test_crate::UninhabitedVariant
original Guarantees: {
    SIZE >= aligned_to(max(offset(0.0) + SizeOf<!>, offset(1.0) + SizeOf<u32>), ALIGN),
    ALIGN >= max(AlignOf<!>, AlignOf<u32>),
    offset of 
        0.0 is aligned to AlignOf<!>,
        1.0 is aligned to AlignOf<u32>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(offset(0.0) + SizeOf<!>, offset(1.0) + SizeOf<u32>), ALIGN),
    ALIGN >= max(AlignOf<!>, AlignOf<u32>),
    offset of 
        0.0 is aligned to AlignOf<!>,
        1.0 is aligned to AlignOf<u32>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0.0) + 0usize, offset(1.0) + 4usize), 4usize),
    ALIGN >= 4usize,
    offset of 
        0.0 is aligned to 1usize,
        1.0 is aligned to 4usize,
}

test_crate::UninhabitedVariant2
original Guarantees: {
    SIZE >= aligned_to(max(offset(0.0) + SizeOf<!>, offset(0.1) + SizeOf<u32>, offset(1.0) + SizeOf<u32>), ALIGN),
    ALIGN >= max(AlignOf<!>, AlignOf<u32>, AlignOf<u32>),
    offset of 
        0.0 is aligned to AlignOf<!>,
        0.1 is aligned to AlignOf<u32>,
        1.0 is aligned to AlignOf<u32>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(offset(0.0) + SizeOf<!>, offset(0.1) + SizeOf<u32>, offset(1.0) + SizeOf<u32>), ALIGN),
    ALIGN >= max(AlignOf<!>, AlignOf<u32>, AlignOf<u32>),
    offset of 
        0.0 is aligned to AlignOf<!>,
        0.1 is aligned to AlignOf<u32>,
        1.0 is aligned to AlignOf<u32>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0.0) + 0usize, offset(0.1) + 4usize, offset(1.0) + 4usize), 4usize),
    ALIGN >= 4usize,
    offset of 
        0.0 is aligned to 1usize,
        0.1 is aligned to 4usize,
        1.0 is aligned to 4usize,
}

test_crate::UninhabitedVariantWithFields
original Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<u32>, offset(1.1) + SizeOf<!>, offset(1.2) + SizeOf<u32>), ALIGN),
    ALIGN >= max(1usize, AlignOf<u32>, AlignOf<!>, AlignOf<u32>),
    offset of 
        1.0 is aligned to AlignOf<u32>,
        1.1 is aligned to AlignOf<!>,
        1.2 is aligned to AlignOf<u32>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<u32>, offset(1.1) + SizeOf<!>, offset(1.2) + SizeOf<u32>), ALIGN),
    ALIGN >= max(1usize, AlignOf<u32>, AlignOf<!>, AlignOf<u32>),
    offset of 
        1.0 is aligned to AlignOf<u32>,
        1.1 is aligned to AlignOf<!>,
        1.2 is aligned to AlignOf<u32>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + 4usize, offset(1.1) + 0usize, offset(1.2) + 4usize), 4usize),
    ALIGN >= 4usize,
    offset of 
        1.0 is aligned to 4usize,
        1.1 is aligned to 1usize,
        1.2 is aligned to 4usize,
}

test_crate::Uninhabited
original Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<!>), ALIGN),
    ALIGN >= max(AlignOf<!>),
    offset of 
        0.0 is aligned to AlignOf<!>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(offset(0) + SizeOf<!>), ALIGN),
    ALIGN >= max(AlignOf<!>),
    offset of 
        0 is aligned to AlignOf<!>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0) + 0usize), 1usize),
    ALIGN >= 1usize,
    offset of 
        0.0 is aligned to 1usize,
}

test_crate::DiscriminantInNicheOfField
original Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<(usize, &'a T)>), ALIGN),
    ALIGN >= max(1usize, AlignOf<(usize, &'a T)>),
    offset of 
        1.0 is aligned to AlignOf<(usize, &'a T)>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<(usize, &'a T)>), ALIGN),
    ALIGN >= max(1usize, AlignOf<(usize, &'a T)>),
    offset of 
        1.0 is aligned to AlignOf<(usize, &'a T)>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + aligned_to(max(offset(0) + 8usize, offset(1) + aligned_to(8usize + SizeOf<PtrMetadata<T>>, max(8usize, SizeOf<PtrMetadata<T>>))), max(8usize, max(8usize, SizeOf<PtrMetadata<T>>)))), max(1usize, max(8usize, max(8usize, SizeOf<PtrMetadata<T>>)))),
    ALIGN >= max(1usize, max(8usize, max(8usize, SizeOf<PtrMetadata<T>>))),
    offset of 
        1.0 is aligned to max(8usize, max(8usize, SizeOf<PtrMetadata<T>>)),
}

test_crate::MaybeUninitInt

test_crate::PackIntsUnion

test_crate::NonZeroNiche
original Guarantees: {
    SIZE >= aligned_to(max(offset(0.0) + SizeOf<char>, 0usize, 0usize), ALIGN),
    ALIGN >= max(AlignOf<char>, 1usize, 1usize),
    offset of 
        0.0 is aligned to AlignOf<char>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(offset(0.0) + SizeOf<char>, 0usize, 0usize), ALIGN),
    ALIGN >= max(AlignOf<char>, 1usize, 1usize),
    offset of 
        0.0 is aligned to AlignOf<char>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0.0) + 4usize, 0usize, 0usize), 4usize),
    ALIGN >= 4usize,
    offset of 
        0.0 is aligned to 4usize,
}

test_crate::ArbitraryDiscriminants
original Guarantees: {
    SIZE = aligned_to(max(offset(0.0) + SizeOf<String>, offset(1.0) + SizeOf<u32>, 4usize), ALIGN),
    ALIGN = max(AlignOf<i32>, AlignOf<String>, AlignOf<i32>, AlignOf<u32>, AlignOf<i32>),
    offset of 
        0.0 is computed by repr(C) algorithm,
        1.0 is computed by repr(C) algorithm,
}
direct Guarantees: {
    SIZE = aligned_to(max(offset(0.0) + SizeOf<String>, offset(1.0) + SizeOf<u32>, 4usize), ALIGN),
    ALIGN = max(AlignOf<i32>, AlignOf<String>, AlignOf<i32>, AlignOf<u32>, AlignOf<i32>),
    offset of 
        0.0 is computed by repr(C) algorithm,
        1.0 is computed by repr(C) algorithm,
}
normalized Guarantees: {
    SIZE = aligned_to(max(aligned_to(4usize, AlignOf<String>) + SizeOf<String>, 8usize, 4usize), max(4usize, AlignOf<String>, 4usize, 4usize, 4usize)),
    ALIGN = max(4usize, AlignOf<String>, 4usize, 4usize, 4usize),
    offset of 
        0.0 is computed by repr(C) algorithm,
        1.0 is computed by repr(C) algorithm,
}

test_crate::MyOrder
original Guarantees: {
    SIZE = 1usize,
    ALIGN = AlignOf<i8>,
}
direct Guarantees: {
    SIZE = 1usize,
    ALIGN = AlignOf<i8>,
}
normalized Guarantees: {
    SIZE = 1usize,
    ALIGN = 1usize,
}

test_crate::WithNicheAndUninhabited
original Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<!>, offset(2.0) + SizeOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>), ALIGN),
    ALIGN >= max(1usize, AlignOf<!>, AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>),
    offset of 
        1.0 is aligned to AlignOf<!>,
        2.0 is aligned to AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<!>, offset(2.0) + SizeOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>), ALIGN),
    ALIGN >= max(1usize, AlignOf<!>, AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>),
    offset of 
        1.0 is aligned to AlignOf<!>,
        2.0 is aligned to AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + 0usize, offset(2.0) + SizeOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>), max(1usize, 1usize, AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>)),
    ALIGN >= max(1usize, 1usize, AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>),
    offset of 
        1.0 is aligned to 1usize,
        2.0 is aligned to AlignOf<NonZero<u32>[{built_in impl Sized for u32}, impl_ZeroablePrimitive_for_u32]>,
}

test_crate::GenericUnsized
original Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<&'a T>), ALIGN),
    ALIGN >= max(1usize, AlignOf<&'a T>),
    offset of 
        1.0 is aligned to AlignOf<&'a T>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<&'a T>), ALIGN),
    ALIGN >= max(1usize, AlignOf<&'a T>),
    offset of 
        1.0 is aligned to AlignOf<&'a T>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + aligned_to(8usize + SizeOf<PtrMetadata<T>>, max(8usize, SizeOf<PtrMetadata<T>>))), max(1usize, max(8usize, SizeOf<PtrMetadata<T>>))),
    ALIGN >= max(1usize, max(8usize, SizeOf<PtrMetadata<T>>)),
    offset of 
        1.0 is aligned to max(8usize, SizeOf<PtrMetadata<T>>),
}

test_crate::GenericButFixedSize
original Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<&'a T>), ALIGN),
    ALIGN >= max(1usize, AlignOf<&'a T>),
    offset of 
        1.0 is aligned to AlignOf<&'a T>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<&'a T>), ALIGN),
    ALIGN >= max(1usize, AlignOf<&'a T>),
    offset of 
        1.0 is aligned to AlignOf<&'a T>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + aligned_to(8usize + SizeOf<PtrMetadata<T>>, max(8usize, SizeOf<PtrMetadata<T>>))), max(1usize, max(8usize, SizeOf<PtrMetadata<T>>))),
    ALIGN >= max(1usize, max(8usize, SizeOf<PtrMetadata<T>>)),
    offset of 
        1.0 is aligned to max(8usize, SizeOf<PtrMetadata<T>>),
}

test_crate::MaxBitsDiscr
original Guarantees: {
    SIZE = 16usize,
    ALIGN = AlignOf<u128>,
}
direct Guarantees: {
    SIZE = 16usize,
    ALIGN = AlignOf<u128>,
}
normalized Guarantees: {
    SIZE = 16usize,
    ALIGN = 16usize,
}

test_crate::SingleVariantButNonZero
original Guarantees: {
    SIZE = SizeOf<Result<!, ()>[{built_in impl Sized for !}, {built_in impl Sized for ()}]>,
    ALIGN = AlignOf<Result<!, ()>[{built_in impl Sized for !}, {built_in impl Sized for ()}]>,
}
direct Guarantees: {
    SIZE = SizeOf<Result<!, ()>[{built_in impl Sized for !}, {built_in impl Sized for ()}]>,
    ALIGN = AlignOf<Result<!, ()>[{built_in impl Sized for !}, {built_in impl Sized for ()}]>,
    offsets of Result<!, ()>[{built_in impl Sized for !}, {built_in impl Sized for ()}]
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0.0) + 0usize, offset(1.0) + 0usize), 1usize),
    ALIGN >= 1usize,
}

test_crate::NonAdtAlias
original Guarantees: {
    SIZE = SizeOf<T>,
    ALIGN = AlignOf<T>,
}
direct Guarantees: {
    SIZE = SizeOf<T>,
    ALIGN = AlignOf<T>,
    offsets of T
}
normalized Guarantees: {
    SIZE = SizeOf<T>,
    ALIGN = AlignOf<T>,
}

test_crate::Tuple
original Guarantees: {
    SIZE = SizeOf<(u32, u32)>,
    ALIGN = AlignOf<(u32, u32)>,
    offset of 
        0.0 is aligned to AlignOf<u32>,
        0.1 is aligned to AlignOf<u32>,
}
direct Guarantees: {
    SIZE = SizeOf<(u32, u32)>,
    ALIGN = AlignOf<(u32, u32)>,
    offsets of (u32, u32)
}
normalized Guarantees: {
    SIZE >= aligned_to(max(offset(0) + 4usize, offset(1) + 4usize), 4usize),
    ALIGN >= 4usize,
    offset of 
        0.0 is aligned to 4usize,
        0.1 is aligned to 4usize,
}

test_crate::Usize
original Guarantees: {
    SIZE = SizeOf<usize>,
    ALIGN = AlignOf<usize>,
}
direct Guarantees: {
    SIZE = SizeOf<usize>,
    ALIGN = AlignOf<usize>,
    offsets of usize
}
normalized Guarantees: {
    SIZE = 8usize,
    ALIGN = 8usize,
}

test_crate::Str
original Guarantees: {
    SIZE = SliceLength,
    ALIGN = AlignOf<u8>,
}
direct Guarantees: {
    SIZE = SliceLength,
    ALIGN = AlignOf<u8>,
    offsets of str
}
normalized Guarantees: {
    SIZE = SliceLength,
    ALIGN = 1usize,
}

test_crate::Ref
original Guarantees: {
    SIZE = SizeOf<&'a mut u32>,
    ALIGN = AlignOf<&'a mut u32>,
}
direct Guarantees: {
    SIZE = SizeOf<&'a mut u32>,
    ALIGN = AlignOf<&'a mut u32>,
    offsets of &'a mut u32
}
normalized Guarantees: {
    SIZE = 8usize,
    ALIGN = 8usize,
}

test_crate::HasInvalidDiscr
original Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<bool>, 0usize), ALIGN),
    ALIGN >= max(1usize, AlignOf<bool>, 1usize),
    offset of 
        1.0 is aligned to AlignOf<bool>,
}
direct Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + SizeOf<bool>, 0usize), ALIGN),
    ALIGN >= max(1usize, AlignOf<bool>, 1usize),
    offset of 
        1.0 is aligned to AlignOf<bool>,
}
normalized Guarantees: {
    SIZE >= aligned_to(max(0usize, offset(1.0) + 1usize, 0usize), 1usize),
    ALIGN >= 1usize,
    offset of 
        1.0 is aligned to 1usize,
}

