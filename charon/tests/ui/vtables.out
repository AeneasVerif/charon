# Final LLBC before serialization:

struct core::marker::MetaSized::{vtable} {
  drop_func: fn(*mut ()),
  self_ty_size: usize,
  self_ty_align: usize,
}

// Full name: core::marker::MetaSized
#[lang_item("meta_sized")]
pub trait MetaSized<Self>

struct test_crate::Checkable::{vtable} {
  drop_func: fn(*mut ()),
  self_ty_size: usize,
  self_ty_align: usize,
  super_trait_0: *const core::marker::MetaSized::{vtable},
  check: fn<'_0>(*mut ()) -> bool,
}

// Full name: test_crate::Checkable
trait Checkable<Self>
{
    parent_clause0 : [@TraitClause0]: MetaSized<Self>
    fn check<'_0> = test_crate::Checkable::check<'_0_0, Self>[Self]
}

fn test_crate::Checkable::check<'_0, Self>(@1: &'_0 (Self)) -> bool
where
    [@TraitClause0]: Checkable<Self>,

// Full name: test_crate::{impl Checkable for i32}::check
fn {impl Checkable for i32}::check<'_0>(@1: &'_0 (i32)) -> bool
{
    let @0: bool; // return
    let self@1: &'_ (i32); // arg #1
    let @2: i32; // anonymous local

    storage_live(@2)
    @2 := copy (*(self@1))
    @0 := move (@2) > const (0 : i32)
    storage_dead(@2)
    return
}

// Full name: test_crate::{impl Checkable for i32}
impl Checkable for i32 {
    parent_clause0 = MetaSized<i32>
    fn check<'_0> = {impl Checkable for i32}::check<'_0_0>
}

// Full name: test_crate::main
fn main()
{
    let @0: (); // return
    let x@1: &'_ ((dyn exists<_dyn> [@TraitClause0]: Checkable<_dyn> + _dyn : '_)); // local
    let @2: &'_ (i32); // anonymous local
    let @3: &'_ (i32); // anonymous local
    let @4: i32; // anonymous local
    let @5: bool; // anonymous local
    let @6: &'_ ((dyn exists<_dyn> [@TraitClause0]: Checkable<_dyn> + _dyn : '_)); // anonymous local

    storage_live(x@1)
    storage_live(@2)
    storage_live(@3)
    storage_live(@4)
    @4 := const (42 : i32)
    @3 := &@4
    @2 := &*(@3)
    x@1 := unsize_cast<&'_ (i32), &'_ ((dyn exists<_dyn> [@TraitClause0]: Checkable<_dyn> + _dyn : '_)), {impl Checkable for i32}>(move (@2))
    storage_dead(@2)
    storage_dead(@3)
    storage_live(@5)
    storage_live(@6)
    @6 := &*(x@1)
    @5 := Checkable<(dyn exists<_dyn> [@TraitClause0]: Checkable<_dyn> + _dyn : '_)>::check<'_>(move (@6))
    storage_dead(@6)
    storage_dead(@5)
    @0 := ()
    storage_dead(@4)
    storage_dead(x@1)
    @0 := ()
    return
}



