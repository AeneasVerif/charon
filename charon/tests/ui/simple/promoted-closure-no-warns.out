# Final LLBC before serialization:

pub fn test_crate::foo() -> &'static (fn(u32) -> u32)
{
    let @0: &'_ (fn(u32) -> u32); // return
    let @1: &'_ (fn(u32) -> u32); // anonymous local
    let @2: &'_ (fn(u32) -> u32); // anonymous local
    let @3: &'_ (fn(u32) -> u32); // anonymous local
    let @4: fn(u32) -> u32; // anonymous local
    let @5: &'_ (fn(u32) -> u32); // anonymous local

    storage_live(@1)
    storage_live(@2)
    @4 := const (Opaque(Unhandled type))
    @5 := &@4
    @3 := move (@5)
    @2 := &*(@3)
    @1 := &*(@2)
    @0 := &*(@1)
    storage_dead(@2)
    storage_dead(@1)
    return
}



