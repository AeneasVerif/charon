# Final LLBC before serialization:

opaque type core::marker::MetaSized::{vtable}

// Full name: core::marker::MetaSized
#[lang_item("meta_sized")]
pub trait MetaSized<Self>

// Full name: core::marker::Sized
#[lang_item("sized")]
pub trait Sized<Self>
{
    parent_clause0 : [@TraitClause0]: MetaSized<Self>
    non-dyn-compatible
}

// Full name: core::marker::Destruct
#[lang_item("destruct")]
pub trait Destruct<Self>
{
    fn drop_in_place = drop_in_place<Self>
    vtable: core::marker::Destruct::{vtable}
}

// Full name: core::marker::Destruct::drop_in_place
unsafe fn drop_in_place<Self>(@1: *mut Self)
= <opaque>

fn UNIT_METADATA()
{
    let @0: (); // return

    @0 := ()
    return
}

const UNIT_METADATA: () = @Fun0()

// Full name: test_crate::Foo
pub trait Foo<Self>
{
    parent_clause0 : [@TraitClause0]: MetaSized<Self>
    vtable: test_crate::Foo::{vtable}
}

// Full name: test_crate::{impl Foo for ()}
impl Foo for () {
    parent_clause0 = {built_in impl MetaSized for ()}
    vtable: {impl Foo for ()}::{vtable}
}

struct test_crate::Broken::{vtable}<Ty0> {
  size: usize,
  align: usize,
  drop: unsafe fn(*mut (dyn Broken<Assoc = Ty0>)),
  method_broken: fn<'_0_1>(&'_0_1 (dyn Broken<Assoc = Ty0>)),
  super_trait_0: &'static core::marker::MetaSized::{vtable},
}

// Full name: test_crate::Broken
pub trait Broken<Self>
{
    parent_clause0 : [@TraitClause0]: MetaSized<Self>
    parent_clause1 : [@TraitClause1]: Sized<Self::Assoc>
    type Assoc
    fn broken<'_0_1> = test_crate::Broken::broken<'_0_1, Self>[Self]
    vtable: test_crate::Broken::{vtable}<Self::Assoc>
}

pub fn test_crate::Broken::broken<'_0, Self>(@1: &'_0 Self)
where
    [@TraitClause0]: Broken<Self>,
= <method_without_default_body>

// Full name: test_crate::{impl Broken for T}::broken
pub fn {impl Broken for T}::broken<'_0, T>(@1: &'_0 T)
where
    [@TraitClause0]: Sized<T>,
    [@TraitClause1]: Foo<()>,
{
    let @0: (); // return
    let self@1: &'0 T; // arg #1

    @0 := ()
    @0 := ()
    return
}

// Full name: test_crate::{impl Broken for T}::{vtable_drop_shim}
unsafe fn {vtable_drop_shim}<T>(@1: *mut (dyn Broken<Assoc = ()>))
where
    [@TraitClause0]: Sized<T>,
{
    let ret@0: (); // return
    let dyn_self@1: *mut (dyn Broken<Assoc = ()> + '0); // arg #1
    let target_self@2: *mut T; // local

    ret@0 := ()
    storage_live(target_self@2)
    target_self@2 := concretize<*mut (dyn Broken<Assoc = ()> + '1), *mut T>(move (dyn_self@1))
    drop[{built_in impl Destruct for T}] *(target_self@2)
    return
}

// Full name: test_crate::{impl Broken for T}::broken::{vtable_method}
fn {vtable_method}<'_0, T>(@1: &'_0 (dyn Broken))
where
    [@TraitClause0]: Sized<T>,
    [@TraitClause1]: Foo<()>,
{
    let @0: (); // return
    let @1: &'_0 (dyn Broken + '0); // arg #1
    let @2: &'_0 T; // anonymous local

    @0 := ()
    storage_live(@2)
    @2 := concretize<&'_0 (dyn Broken + '1), &'_0 T>(move (@1))
    @0 := {impl Broken for T}::broken<'_0, T>[@TraitClause0, @TraitClause1](move (@2))
    return
}

// Full name: test_crate::{impl Broken for T}::{vtable}
fn {impl Broken for T}::{vtable}<T>() -> test_crate::Broken::{vtable}<()>
where
    [@TraitClause0]: Sized<T>,
{
    let ret@0: test_crate::Broken::{vtable}<()>; // return
    let size@1: usize; // local
    let align@2: usize; // local

    storage_live(size@1)
    size@1 := size_of<T>
    storage_live(align@2)
    align@2 := align_of<T>
    ret@0 := test_crate::Broken::{vtable} { size: move (size@1), align: move (align@2), drop: const ({vtable_drop_shim}<T>[@TraitClause0]), method_broken: const ({vtable_method}<'1, T>[@TraitClause0]), super_trait_0: const (Opaque(missing supertrait vtable)) }
    return
}

// Full name: test_crate::{impl Broken for T}::{vtable}
static {impl Broken for T}::{vtable}<T>: test_crate::Broken::{vtable}<()>
where
    [@TraitClause0]: Sized<T>,
 = {impl Broken for T}::{vtable}()

// Full name: test_crate::{impl Broken for T}
impl<T> Broken for T
where
    [@TraitClause0]: Sized<T>,
{
    parent_clause0 = @TraitClause0::parent_clause0
    parent_clause1 = {built_in impl Sized for ()}
    type Assoc = ()
    fn broken<'_0_1, [@TraitClause0_1]: Foo<()>> = {impl Broken for T}::broken<'_0_1, T>[@TraitClause0, @TraitClause0_1]
    vtable: {impl Broken for T}::{vtable}<T>[@TraitClause0]
}

// Full name: test_crate::main
fn main()
{
    let @0: (); // return
    let _m@1: &'0 (dyn Broken<Assoc = ()> + '1); // local
    let @2: &'2 (); // anonymous local
    let @3: &'2 (); // anonymous local
    let @4: &'2 (); // anonymous local
    let @5: &'_ (); // anonymous local
    let @6: (); // anonymous local

    storage_live(@5)
    storage_live(@6)
    @6 := ()
    @5 := &@6
    storage_live(@4)
    @0 := ()
    storage_live(_m@1)
    storage_live(@2)
    storage_live(@3)
    @4 := move (@5)
    @3 := &*(@4)
    @2 := &*(@3)
    _m@1 := unsize_cast<&'2 (), &'0 (dyn Broken<Assoc = ()> + '1), {impl Broken for T}<()>[{built_in impl Sized for ()}] with {impl Broken for T}::{vtable}<()>[{built_in impl Sized for ()}]>(move (@2))
    storage_dead(@2)
    storage_dead(@3)
    @0 := ()
    storage_dead(_m@1)
    return
}



